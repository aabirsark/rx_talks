import 'package:flutter/material.dart';

// ? app name
const String appName = "Roarx Chat";

// ? colors
const Color primaryColor = Colors.green;
final Color scaffoldBgColor = Colors.grey.shade900;

// ? durations
const Duration defaultAnimationDuration = Duration(milliseconds: 500);
const Duration lonAnimationDuration = Duration(milliseconds: 1000);
const Duration defaultDelayDuration = Duration(milliseconds: 400);
